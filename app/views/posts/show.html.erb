<p style="color: green"><%= notice %></p>

<%= render @post %>

<div>
  <%= link_to "Edit Post", edit_shark_post_path(@shark, @post) %> |
  <%= link_to "Back", shark_posts_path(@shark) %>

  <%= button_to "Destroy Post", [@shark, @post], method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
